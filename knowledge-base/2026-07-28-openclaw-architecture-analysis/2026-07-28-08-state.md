# 08 — SQLite 状态管理

## 8.1 核心规则(AGENTS.md "Storage default")

AGENTS.md 明文规定:

> Storage default: SQLite only. Do not add JSON/JSONL/TXT/sidecar files for OpenClaw-owned runtime state, caches, queues, registries, indexes, cursors, checkpoints, or plugin scratch data.

> Any SQLite change requiring a schema-version bump needs explicit user discussion and acceptance before implementation. Agents must not advance SQLite schema versions autonomously.

## 8.2 双层结构

| DB | 路径 | 用途 |
|---|---|---|
| 共享 state DB | `state/openclaw.sqlite` | 全局运行时状态 + 插件 KV 数据 |
| Per-agent DB | `agents/<agentId>/agent/openclaw-agent.sqlite` | Agent-scoped 状态/cache |

证据:
- [src/state/openclaw-state-db.ts](file:///d:/DevSpace/person/ai_space/openclaw/src/state/openclaw-state-db.ts) — 共享 state DB 主实现
- [src/state/openclaw-agent-db.ts](file:///d:/DevSpace/person/ai_space/openclaw/src/state/openclaw-agent-db.ts) — per-agent DB 主实现
- [src/state/openclaw-state-db.paths.ts](file:///d:/DevSpace/person/ai_space/openclaw/src/state/openclaw-state-db.paths.ts) — 共享 DB 路径
- [src/state/openclaw-agent-db.paths.ts](file:///d:/DevSpace/person/ai_space/openclaw/src/state/openclaw-agent-db.paths.ts) — agent DB 路径

## 8.3 Schema 演进

### 当前版本

[package.json](file:///d:/DevSpace/person/ai_space/openclaw/package.json#L4-L9):

```json
"openclaw": {
  "schemaVersions": {
    "state": 6,
    "agent": 16
  }
}
```

- state DB 已演进到第 6 代
- agent DB 已演进到第 16 代(说明 agent 范围变更更频繁)

### 版本 bump 规则

AGENTS.md 明确:

> Any SQLite change requiring a schema-version bump needs explicit user discussion and acceptance before implementation. Agents must not advance SQLite schema versions autonomously.

> Purely additive SQLite surface (new tables; downgraded builds keep working, just without the new feature): do not bump the schema version. Declare in the canonical schema file plus a one-time idempotent lazy ensure on first feature use; fold into the migration path at the next natural bump. Bumps are reserved for changes older readers cannot tolerate.

**关键区分**:
- **纯 additive**(新表,旧版本 build 仍能工作)→ **不 bump**,只在 canonical schema 文件声明 + 首次使用时 lazy ensure,下一次自然 bump 时合并到 migration path。
- **破坏性变更**(旧 reader 无法容忍)→ 必须 bump,需 owner 显式确认。

## 8.4 访问规则

### Kysely helpers,非 raw SQL

AGENTS.md:

> SQLite runtime access uses Kysely helpers, not raw SQL statement strings, except schema DDL, migrations, low-level DB bootstrap, or narrowly justified SQLite primitives.

### 写事务同步 commit

AGENTS.md:

> SQLite write transactions are synchronous commit sections only. Finish async planning, filesystem access, plugin hooks, and predicates before `BEGIN`; then reread and validate authoritative rows before writing. Never return a Promise or execute `await` from a transaction callback.

**关键约束**:
- BEGIN 之前完成所有 async 工作(planning、fs 访问、plugin hooks、predicates)
- 写入前重读校验权威行
- **禁止从 transaction callback 返回 Promise 或执行 await**

## 8.5 关键文件分布

### 共享 state DB 系列

| 文件 | 职责 |
|---|---|
| `openclaw-state-db.ts` | 主实现 |
| `openclaw-state-db.paths.ts` | 路径 |
| `openclaw-state-db-startup-checkpoint.ts` | 启动 checkpoint |
| `openclaw-state-db-session-watch-migration.ts` | session watch 迁移 |
| `openclaw-state-db-schema-repair.ts` | schema 修复 |
| `openclaw-state-db-schema-helpers.ts` | schema 助手 |
| `openclaw-state-db-schema-additive.ts` | additive schema |
| `openclaw-state-db-readonly.ts` | 只读访问 |
| `openclaw-state-db-permissions.ts` | 权限 |
| `openclaw-state-db-operator-approval-migration.ts` | operator approval 迁移 |
| `openclaw-state-db-maintenance.ts` | 维护 |
| `openclaw-state-db-legacy-backfills.ts` | legacy 回填 |
| `openclaw-state-db-contract.ts` | 契约 |
| `openclaw-state-db-audit-migration.ts` | audit 迁移 |
| `openclaw-state-schema.generated.ts` | 生成 schema |
| `openclaw-state-snapshot-sanitizer.ts` | 快照 sanitizer |
| `openclaw-state-lease.ts` | 租约 |
| `openclaw-schema-versions.ts` | schema 版本常量 |
| `openclaw-database-verify.ts` / `.impl.ts` / `.worker.ts` | 数据库校验 |
| `openclaw-database-preflight.ts` | 预检 |
| `openclaw-quarantine-store.ts` | 隔离存储 |

### Per-agent DB 系列

| 文件 | 职责 |
|---|---|
| `openclaw-agent-db.ts` | 主实现 |
| `openclaw-agent-db.paths.ts` | 路径 |
| `openclaw-agent-db-schema.ts` | schema |
| `openclaw-agent-db-schema-helpers.ts` | schema 助手 |
| `openclaw-agent-db-registry.ts` / `-registry-listing.ts` | 注册与列表 |
| `openclaw-agent-db-readonly.ts` | 只读 |
| `openclaw-agent-db-permissions.ts` | 权限 |
| `openclaw-agent-db-maintenance.ts` | 维护 |
| `openclaw-agent-db-lease.ts` | 租约 |
| `openclaw-agent-db-contract.ts` | 契约 |
| `openclaw-agent-db-session-provenance.ts` | session 来源 |
| `openclaw-agent-db-session-nodes-migration.ts` | session nodes 迁移 |
| `openclaw-agent-db-session-migrations.ts` | session 迁移 |
| `openclaw-agent-board-schema.ts` | 看板 schema |
| `openclaw-agent-schema.generated.ts` | 生成 schema |
| `openclaw-agent-session-sharing-schema.ts` | session 共享 schema |

### 其他状态文件

- `user-profiles.ts` / `user-profiles-schema.ts` — 用户档案
- `config-machine-state.ts` — 配置机器状态
- `onboarding-recommendations.ts` — onboarding 推荐
- `claw-package-lifecycle-lease.ts` / `claw-package-adoption.ts` / `claw-mcp-adoption.ts` — 包生命周期
- `agent-deletion-journal.ts` — agent 删除日志
- `control-ui-device-auth-migration.ts` — control UI 设备 auth 迁移
- `session-watch-cursor-provenance.ts` — session watch cursor 来源

## 8.6 迁移策略

AGENTS.md 明确:

> State/storage migrations are database-first. Runtime reads/writes the canonical store only. Old file stores, sidecars, aliases, and fallback readers belong in `openclaw doctor --fix` migration code only, never steady-state runtime.

> Persistent user state gets one migration owner. Doctor migrates, verifies, and then runtime assumes the new shape. No dual-write, read-through fallback, lazy import, or "if SQLite fails use JSON" branches.

> Cache/transient state gets no compat migration unless a shipped user contract is cited. Prefer delete/drop/rebuild over import. If old state can be lost without user-visible data loss, remove the old path entirely.

**关键原则**:
1. **数据库优先**:runtime 只读写 canonical store,不读旧 file stores/sidecars/aliases。
2. **Doctor 单一迁移 owner**:Doctor 迁移 → 校验 → runtime 假定新形状;**禁止 dual-write、read-through fallback、lazy import、"if SQLite fails use JSON"**。
3. **Cache 无 compat migration**:除非有 shipped 用户契约,否则删除/重建而非 import。

## 8.7 Fallback 决策

AGENTS.md:

> Fallback is a product decision, not an implementation convenience. Before adding one, name the shipped contract, failure mode, removal plan, and why doctor cannot solve it. Otherwise delete it.

> If unsure, ask before preserving compat. Do not keep aliases, shims, fallback stacks, stale names, or obsolete tests just in case.

## 8.8 关键观察

- **SQLite only**:杜绝散落 JSON 文件,所有运行时状态集中管理。
- **schema 版本严控**:agent 不可自主 bump,需 owner 显式确认。
- **写事务同步**:BEGIN 前完成 async 工作,写入前重读校验,禁止 await in callback。
- **迁移单一 owner**:Doctor 迁移后 runtime 假定新形状,无 dual-write。
- **大量 schema 演进**:state=6, agent=16,迁移代码累积可观(各种 `*-migration.ts`、`*-backfills.ts`、`*-repair.ts`)。

## 架构图与流程图

### 双层 SQLite 结构示意图

```
    OpenClaw 状态层(SQLite only)
    ═══════════════════════════════

    ┌──────────────────────────────────────────────────────────────┐
    │                    共享 state DB                              │
    │                state/openclaw.sqlite                         │
    │                    schema v6                                 │
    │                                                              │
    │  ┌──────────────────────────────────────────────────────┐   │
    │  │ 全局运行时状态                                       │   │
    │  │  • gateway 配置缓存                                 │   │
    │  │  • channel 状态                                     │   │
    │  │  • plugin 注册表                                    │   │
    │  │  • session watch cursor                             │   │
    │  │  • cron 任务状态                                    │   │
    │  └──────────────────────────────────────────────────────┘   │
    │                                                              │
    │  ┌──────────────────────────────────────────────────────┐   │
    │  │ 插件 KV 数据                                         │   │
    │  │  • 各插件的键值存储                                  │   │
    │  │  • 跨 agent 共享的插件数据                           │   │
    │  └──────────────────────────────────────────────────────┘   │
    │                                                              │
    │  ┌──────────────────────────────────────────────────────┐   │
    │  │ 其他共享状态                                         │   │
    │  │  • user-profiles                                     │   │
    │  │  • config-machine-state                              │   │
    │  │  • onboarding-recommendations                        │   │
    │  │  • claw-package-lifecycle-lease                      │   │
    │  │  • agent-deletion-journal                            │   │
    │  └──────────────────────────────────────────────────────┘   │
    └──────────────────────────────────────────────────────────────┘

                            ▲
                            │ 所有层读写
                            │
    ┌──────────────────────────────────────────────────────────────┐
    │                    Per-agent DB                              │
    │        agents/<agentId>/agent/openclaw-agent.sqlite          │
    │                    schema v16                                │
    │                                                              │
    │  ┌──────────────────────────────────────────────────────┐   │
    │  │ Agent-scoped 状态                                    │   │
    │  │  • session history                                  │   │
    │  │  • conversation turns                               │   │
    │  │  • tool call records                                 │   │
    │  └──────────────────────────────────────────────────────┘   │
    │                                                              │
    │  ┌──────────────────────────────────────────────────────┐   │
    │  │ Agent-scoped cache                                  │   │
    │  │  • prompt cache                                      │   │
    │  │  • tool descriptor cache                             │   │
    │  │  • model catalog cache                               │   │
    │  └──────────────────────────────────────────────────────┘   │
    │                                                              │
    │  ┌──────────────────────────────────────────────────────┐   │
    │  │ Agent board schema                                   │   │
    │  │  • session sharing                                   │   │
    │  │  • session provenance                                │   │
    │  └──────────────────────────────────────────────────────┘   │
    └──────────────────────────────────────────────────────────────┘

    ❌ 禁止的存储方式:
    ┌──────────────────────────────────────────────────────────────┐
    │  • JSON 文件(运行时状态)                                   │
    │  • JSONL 文件(队列/cursor/checkpoint)                     │
    │  • TXT 文件                                                 │
    │  • sidecar 文件                                             │
    │  • 任何 OpenClaw-owned runtime state 的非 SQLite 存储       │
    └──────────────────────────────────────────────────────────────┘
```

### 写事务同步 commit 流程图

```
    SQLite 写事务流程(硬约束!)
    ═════════════════════════════

    ┌──────────────────────────────────────────────────────────┐
    │ 阶段 1:Async Planning(BEGIN 之前)                       │
    │                                                          │
    │  ✅ 允许:                                                │
    │  ├─ 异步规划                                            │
    │  ├─ 文件系统访问                                        │
    │  ├─ plugin hooks                                        │
    │  └─ predicates(谓词判断)                              │
    │                                                          │
    │  ⚠️ 必须:                                                │
    │  └─ 所有 async 工作在 BEGIN 之前完成                    │
    └──────────────────────┬───────────────────────────────────┘
                           │
                           ▼
    ┌──────────────────────────────────────────────────────────┐
    │ 阶段 2:BEGIN Transaction                               │
    │                                                          │
    │  开始同步 commit section                                 │
    │                                                          │
    │  ⚠️ 从这里开始:                                         │
    │     ❌ 禁止返回 Promise                                 │
    │     ❌ 禁止执行 await                                   │
    │     ❌ 禁止任何异步操作                                 │
    └──────────────────────┬───────────────────────────────────┘
                           │
                           ▼
    ┌──────────────────────────────────────────────────────────┐
    │ 阶段 3:重读 + 校验权威行                               │
    │                                                          │
    │  ✅ 必须:                                                │
    │  ├─ 重读要修改的行                                      │
    │  └─ 校验行未被其他事务修改                              │
    │                                                          │
    │  原因:                                                  │
    │  ├─ 防止 lost update                                    │
    │  └─ 确保写入基于最新数据                                │
    └──────────────────────┬───────────────────────────────────┘
                           │
                           ▼
    ┌──────────────────────────────────────────────────────────┐
    │ 阶段 4:写入(同步)                                     │
    │                                                          │
    │  使用 Kysely helpers(非 raw SQL)                       │
    │                                                          │
    │  例外(允许 raw SQL):                                   │
    │  ├─ schema DDL                                          │
    │  ├─ migrations                                          │
    │  ├─ low-level DB bootstrap                              │
    │  └─ narrowly justified SQLite primitives                │
    └──────────────────────┬───────────────────────────────────┘
                           │
                           ▼
    ┌──────────────────────────────────────────────────────────┐
    │ 阶段 5:COMMIT(同步)                                   │
    │                                                          │
    │  同步 commit,事务完成                                   │
    └──────────────────────────────────────────────────────────┘

    违反约束的后果:
    ┌────────────────────────────────────────────────────────┐
    │ 如果在 transaction callback 中 await:                 │
    │  • SQLite 可能锁定                                    │
    │  • 事务可能超时                                       │
    │  • 数据一致性可能破坏                                 │
    │  • 性能严重下降                                       │
    │                                                        │
    │ AGENTS.md 明确:                                       │
    │   "Never return a Promise or execute `await`          │
    │    from a transaction callback."                       │
    └────────────────────────────────────────────────────────┘
```

### Schema 演进与迁移策略图

```
    旧版本运行时(用户已有)
         │
         │ 配置/数据是旧 shape
         ▼
    ┌──────────────────────────────────────────────────────────┐
    │ Runtime 阶段(正常运行时)                               │
    │                                                          │
    │  ❌ 不读旧 file stores                                  │
    │  ❌ 不读 sidecars                                       │
    │  ❌ 不读 aliases                                        │
    │  ❌ 不做 fallback readers                                │
    │  ❌ 不做 dual-write                                     │
    │  ❌ 不做 read-through fallback                          │
    │  ❌ 不做 lazy import                                    │
    │  ❌ 不做 "if SQLite fails use JSON"                     │
    │                                                          │
    │  ✅ 只读 canonical store(最新 shape)                  │
    └──────────────────────────────────────────────────────────┘

    需要升级时:
    ┌──────────────────────────────────────────────────────────┐
    │ 用户运行:openclaw doctor --fix                         │
    │                                                          │
    │  Doctor 流程(单一迁移 owner):                         │
    │  ├─ 1. 读取旧 shape 数据                                │
    │  ├─ 2. 迁移到新 shape                                   │
    │  ├─ 3. 校验新 shape 正确                                │
    │  └─ 4. 标记迁移完成                                     │
    │                                                          │
    │  迁移后:                                                │
    │  └─ Runtime 假定新 shape,不再回头看旧数据              │
    └──────────────────────────────────────────────────────────┘

    Schema 变更决策:
    ┌──────────────────────────────────────────────────────────┐
    │ 想变更 SQLite schema?                                   │
    │                                                          │
    │ ├─ 纯 additive?(新表,旧版本 build 仍能工作)          │
    │ │   └─ Yes → 不 bump schema version                     │
    │ │       ├─ 在 canonical schema 文件声明                 │
    │ │       ├─ 首次使用时 lazy ensure(idempotent)          │
    │ │       └─ 下一次自然 bump 时合并到 migration path     │
    │ │                                                        │
    │ ├─ 破坏性?(旧 reader 无法容忍)                        │
    │ │   └─ Yes → 必须 bump schema version                  │
    │ │       ├─ ⚠️ 需要 explicit user discussion            │
    │ │       ├─ ⚠️ 需要 acceptance before implementation    │
    │ │       └─ ⚠️ Agents must not advance autonomously     │
    │ │                                                        │
    │ └─ Cache/transient state?                               │
    │     └─ 无 compat migration(除非有 shipped 契约)       │
    │         ├─ Prefer delete/drop/rebuild over import       │
    │         └─ 如果旧 state 可丢失,删除旧 path             │
    └──────────────────────────────────────────────────────────┘

    迁移代码位置:
    ┌──────────────────────────────────────────────────────────┐
    │ Core/auth config repairs                                │
    │  → core doctor(在核心代码中)                          │
    │                                                          │
    │ Plugin-owned config repairs                              │
    │  → plugin doctor contract                               │
    │    (legacyConfigRules / normalizeCompatibilityConfig)   │
    └──────────────────────────────────────────────────────────┘
```
